package simulation.View;

/**
 *
 * @author tunyaa
 */
public class View implements Renderer{

    @Override
    public void render() {
//        GraphicsContext gc = canvas.getGraphicsContext2D();
//        gc.setFill(Color.BLACK);     // цвет заливки
//        int x = 0;// Переменная для движения по массиву
//        
//        for (int i = 1; i <= worldMap.getHeight(); i++) {
//            for (int j = 1; j <= worldMap.getWidth(); j++) {
//                
//                Integer get = worldMap.getPositions().get(x++);// получаю элемент из массива
//                gc.fillText(String.valueOf(get), 3 + j* 6, 3 + i* 6);   // Вывожу символ по координате.
//            }
//        }
    }
    
}
